<h1>"Title"</h1>



<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/genie-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>


<div class="burgersDevoured">
<h2>"TITLE"</h2>
</div>

<ul>
  {{#each genie}}
    {{#if devour}}
      {{> burgers/genie-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h3>"TITLE"</h3>


<form class="create-form">

  <div class="form-group-1">
    <label for="bn">Burger Name:</label>
    <input type="text" id="bn" name="name">
  </div>

  <div class="form-group-2">
    <label for="currentBurger">Your Burger</label><br>
    <input type="radio" name="currentBurger" value="1" checked> Burger Time!<br>
  
  </div>

  <button type="submit">Add Your Burger!</button>
</form>
